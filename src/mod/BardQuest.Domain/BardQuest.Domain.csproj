<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- ns2.1 only: this is what the Mod (Unity/Mono) loads and what YARG.Core targets, so tests
         exercise the same build that ships. -->
    <TargetFramework>netstandard2.1</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../../../lib/YARG.Core/YARG.Core/YARG.Core.csproj" />
  </ItemGroup>
</Project>
